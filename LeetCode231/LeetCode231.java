import java.lang.*;

class Solution {
    public boolean isPowerOfTwo(int n) {
        double root = Math.log(n) / Math.log(2);
        system.out.print(root);

        if (root - (int) root == Math.pow(10, -10))
            return true;
        else
            return false;
    }
}

